[phases.setup]
nixPkgs = ["python312", "chromium", "playwright-driver"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "python -m playwright install --with-deps chromium"
]

[start]
cmd = "python bot.py --run --headless"
